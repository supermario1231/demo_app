<%= form_for(@micro) do |f| %>
  <% if @micro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micro.errors.count, "error") %> prohibited this micro from being saved:</h2>

      <ul>
      <% @micro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
